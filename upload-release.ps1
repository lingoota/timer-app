 = 281753483
 = @{
    'Authorization' = "Bearer  = Join-Path  = "https://uploads.github.com/repos/lingoota/timer-app/releases/ -Method Post -Headers  -ContentType 'application/octet-stream' | Select-Object name, state, size | Format-List

Write-Host 'Uploading latest.yml...'
 'latest.yml'
/assets?name=latest.yml"
Invoke-RestMethod -Uri  -InFile  = Join-Path  = "https://uploads.github.com/repos/lingoota/timer-app/releases/ -Method Post -Headers  -ContentType 'application/octet-stream' | Select-Object name, state, size | Format-List

Write-Host 'Done\!'
